# Where the dev server proxies /api and /socket.io (your chat-backend).
VITE_BACKEND_URL=http://localhost:4001

# Production only: full backend URL if the frontend is hosted on a different origin.
# Leave empty when the backend is served from the same origin (recommended).
VITE_API_URL=
