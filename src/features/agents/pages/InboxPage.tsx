import { Inbox } from 'lucide-react'
import { EmptyState } from '../../../components/ui/EmptyState'

/** Built in step F3. */
export const InboxPage = () => (
  <EmptyState icon={<Inbox size={22} />} title="Agent inbox">
    Coming in step F3.
  </EmptyState>
)
