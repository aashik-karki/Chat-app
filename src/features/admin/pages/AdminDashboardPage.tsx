import { BarChart3 } from 'lucide-react'
import { EmptyState } from '../../../components/ui/EmptyState'

/** Built in step F4. */
export const AdminDashboardPage = () => (
  <EmptyState icon={<BarChart3 size={22} />} title="Admin dashboard">
    Coming in step F4.
  </EmptyState>
)
